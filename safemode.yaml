services:
    frontend:
        extends:
            file: common.yaml
            service: frontend
        command: /bin/bash

    engine:
        extends:
            file: common.yaml
            service: engine
        command: /bin/bash